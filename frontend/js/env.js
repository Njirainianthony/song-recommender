/**
 * env.js
 * -----------------------------------------------------------------------------
 * ACTUAL local configuration (this file is gitignored — see .gitignore).
 *
 * This copy was generated from env.example.js so the app works immediately
 * after cloning. Feel free to edit the values below for your own machine
 * or deployment; it will not affect anyone else's copy.
 * -----------------------------------------------------------------------------
 */
window.APP_CONFIG = {
  // Base URL of your backend API (FastAPI/Flask/etc).
  // Change this when:
  //   - your backend runs on a different port locally
  //   - you deploy the backend to a staging or production server
  API_BASE: "http://127.0.0.1:8000",
};
